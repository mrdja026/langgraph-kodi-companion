## 1. Project Scaffolding- [x] 1.1 Scaffold `agent/` with uv only (`uv init` + src layout): `pyproject.toml`, committed `uv.lock`, `src/agent/` package (`__init__.py`, `config.py`, `llm.py`, `tools.py`, `graph.py`, `main.py`) — no pip/poetry/conda anywhere- [x] 1.2 Add agent dependencies via `uv add`: `langgraph`, `langgraph-checkpoint-sqlite`, `langchain-core`, `langchain-openai`, `langchain-mcp-adapters`, `langchain-community`, `ddgs`, `langsmith`; dev dependency via `uv add --dev "langgraph-cli[inmem]"`- [x] 1.3 Create `agent/langgraph.json`: `{"dependencies": ["."], "graphs": {"agent": "./src/agent/graph.py:graph"}, "env": ".env"}`- [x] 1.4 Scaffold `server/` as a Hono TypeScript project: `package.json`, strict `tsconfig.json`, `src/index.ts`, `src/mcp.ts`, `src/config.ts`, `src/tools/watchlist.ts`, `src/tools/series.ts`, `test/`; deps `hono`, `@hono/node-server`, `@hono/mcp`, `@modelcontextprotocol/sdk`, `zod`; dev tooling `typescript`, `tsx`, `vitest`, lint/format; npm scripts `dev`/`build`/`start`/`test`/`lint`- [x] 1.5 Implement `agent/src/agent/config.py` and `server/src/config.ts` reading env vars with defaults: `LLM_BASE_URL` (http://localhost:8000/v1), `LLM_API_KEY` (local), `LLM_MODEL`, `LLM_TIMEOUT_S` (120), `MCP_SERVER_URL` (http://localhost:3001/mcp), `MCP_TIMEOUT_S` (30), `MCP_BIND_HOST` (127.0.0.1), `MCP_BIND_PORT` (3001), `WATCHLIST_ROOT`, `MEDIA_ROOT`, `CHECKPOINT_DB` (agent/checkpoints.sqlite), `AGENT_RECURSION_LIMIT` (25), `LANGSMITH_TRACING`, `LANGSMITH_API_KEY`, `LANGSMITH_PROJECT` (langgraph-mcp-agent)- [x] 1.6 Add `.env.example` files for both projects documenting every variable above## 2. Hono MCP Tool Server (server/)- [x] 2.1 Implement `src/index.ts`: Hono app bootstrapped with `@hono/node-server`, binding `MCP_BIND_HOST:MCP_BIND_PORT`- [x] 2.2 Implement `src/mcp.ts`: `McpServer` from `@modelcontextprotocol/sdk` bridged to the Hono `/mcp` route via `@hono/mcp` `StreamableHTTPTransport`, responding to `initialize` and `tools/list`- [x] 2.3 Register `read_watchlist` with zod input schema `{ directory_path: string }` and a description telling the model it returns raw watchlist file contents- [x] 2.4 Implement `src/tools/watchlist.ts` (real read, not a stub): resolve `directory_path` against `WATCHLIST_ROOT`, reject path traversal outside the root, return raw per-file contents attributed by filename; missing directory returns an MCP tool error (not a transport failure)- [x] 2.5 Register `download_and_format_series` with zod input schema `{ series_name: string, seasons?: number = 1, episodes_per_season?: number = 3 }`; missing `series_name` yields JSON-RPC invalid-params with no disk writes- [x] 2.6 Implement `src/tools/series.ts` as an explicit STUB: no real download or network media transfer; sanitize `series_name` deterministically for host-OS paths; create placeholder files in `MEDIA_ROOT/<Show Name>/Season <NN>/S<NN>E<NN>.mp4` layout (zero-padded); return created paths plus a placeholder-data notice; confine all writes to `MEDIA_ROOT`- [x] 2.7 Write vitest unit tests: path traversal guard, missing directory error, series-name sanitization, placeholder layout generation, invalid-params rejection- [x] 2.8 Verify the server standalone: `npm run dev` with no agent running, confirm `initialize` + `tools/list` succeed from a scratch MCP client and both tools appear with correct schemas; confirm `npm run build` passes under strict TypeScript## 3. Agent — Tool Layer (agent/)- [x] 3.1 Implement MCP tool binding in `src/agent/tools.py`: `MultiServerMCPClient` (streamable HTTP) against `MCP_SERVER_URL`, converting discovered tools to LangChain tools with `MCP_TIMEOUT_S` applied- [x] 3.2 Implement startup fail-fast check: exit with an error naming the missing tool if `read_watchlist` or `download_and_format_series` is absent from discovery- [x] 3.3 Add the DuckDuckGo search tool (langchain-community + `ddgs`, no API key) with a description directing the model to use it for current/external information; it runs in-process and is never registered on the MCP server- [x] 3.4 Implement error mapping: transport failures, timeouts, and JSON-RPC errors (with server code/message) surface as tool result messages — never unhandled exceptions; DuckDuckGo failures return their error text as the tool result- [x] 3.5 Implement retry policy: at most one retry for read-only MCP calls (`read_watchlist`); never auto-retry `download_and_format_series`## 4. Agent — Inference Layer (agent/)- [x] 4.1 Implement `src/agent/llm.py`: `ChatOpenAI` (or equivalent OpenAI-compatible chat model) configured with `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL`, `LLM_TIMEOUT_S`, supporting native tool calling (`tools` param out, `tool_calls` in)- [x] 4.2 Implement startup health check: verify the endpoint is reachable (e.g., list models) before opening the REPL, with a clear error naming `LLM_BASE_URL` on failure- [x] 4.3 Document the model requirement: the served open-weights model must support OpenAI-style tool calling (e.g., vLLM with `--enable-auto-tool-choice` and an appropriate `--tool-call-parser`)- [x] 4.4 Verify malformed tool-call handling: schema-invalid arguments come back as tool error messages the model can correct in-loop, bounded by the recursion limit## 5. Agent — ReAct Loop, Memory, Chat Surfaces (agent/)- [x] 5.1 Implement `src/agent/graph.py`: `create_react_agent` with the chat model, the three bound tools, and a system prompt describing the media-assistant role and each tool's purpose; export a module-level `graph` WITHOUT a custom checkpointer (Studio-compatible, referenced by `langgraph.json`)- [x] 5.2 Implement the CLI entry `src/agent/main.py`: compile/wrap the agent WITH `SqliteSaver` using `CHECKPOINT_DB` (auto-created on first run), accept a `--thread` id (new or existing), run startup health checks (LLM reachability, MCP tool discovery), then read-eval-print user turns; run via `uv run python -m agent.main --thread <id>`- [x] 5.3 Apply `AGENT_RECURSION_LIMIT` (default 25) to the loop; hitting the limit produces an explanatory assistant message and leaves the thread usable- [x] 5.4 Ensure tool and inference failures print as assistant-visible errors without crashing the REPL- [x] 5.5 Verify LangGraph Studio: `uv sync` then `uv run langgraph dev` boots the local LangGraph API server, lists the `agent` graph, and Studio can hold a multi-turn chat that exercises dynamic tool selection- [x] 5.6 Verify multi-turn context (both surfaces): a follow-up question is answered from earlier tool results in the thread history without necessarily re-invoking tools- [x] 5.7 Verify persistence: (CLI) kill the agent after several turns, relaunch with the same `--thread`, conversation resumes with full context; (Studio) restart the dev server and reopen the thread with history intact; confirm CLI and Studio thread stores are independent and two distinct threads are isolated## 6. Observability- [x] 6.1 Wire LangSmith env configuration (`LANGSMITH_TRACING`, `LANGSMITH_API_KEY`, `LANGSMITH_PROJECT` default `langgraph-mcp-agent`); when the key is absent, run untraced and log a startup warning- [x] 6.2 Attach `thread_id` to run metadata on every turn so conversations are filterable in LangSmith- [x] 6.3 Confirm per-turn traces: parent run per turn containing one LLM run per ReAct iteration and a tool run for every tool execution (MCP and DuckDuckGo), including failed calls with error status- [x] 6.4 Confirm LLM runs capture request messages (with tool schemas), raw completions (with `tool_calls`), model id, and token usage- [x] 6.5 Verify telemetry is non-blocking: with LangSmith unreachable, a turn completes with the same assistant output and intact conversation state## 7. End-to-End Verification- [x] 7.1 Create test fixtures: a `WATCHLIST_ROOT` with sample watchlist text files under "movies/tv shows to watch", and an empty `MEDIA_ROOT`- [x] 7.2 Verify a multi-turn chat scenario on one thread: (turn 1) ask what's on the watchlist → agent calls `read_watchlist`; (turn 2) ask which listed show has a new season → agent calls DuckDuckGo search; (turn 3) ask to download one → agent calls `download_and_format_series` and the placeholder layout (e.g., `Dark/Season 01/S01E01.mp4`) appears under `MEDIA_ROOT`- [x] 7.3 Verify a no-tool turn (e.g., "summarize our conversation") produces a direct answer with no tool runs- [x] 7.4 Verify error paths: MCP server down mid-chat (assistant reports tool service unavailable, next turn recovers once restarted); LLM endpoint down (turn errors visibly, REPL survives); path traversal attempt rejected by the server; DuckDuckGo failure degrades gracefully- [x] 7.5 Verify LangSmith traces for the multi-turn scenario: per-turn parent runs with LLM and tool spans, `thread_id` metadata filtering returns exactly that conversation's turns- [x] 7.6 Write README(s) documenting: uv-only workflow for the agent (`uv sync`, `uv run`, never pip); the three-process startup order (model server → Hono MCP server → agent); both chat surfaces (`uv run langgraph dev` for Studio, `uv run python -m agent.main --thread <id>` for CLI) and their separate thread stores; all environment variables; the tool-calling model requirement; and the localhost-only security posture of the MCP server